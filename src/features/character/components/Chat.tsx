import { Flex } from "@chakra-ui/react";

interface IChatProps {
}

const Chat: React.FC<IChatProps> = (props) => {
  return (
    <Flex height="100vh">
        CHAT
    </Flex>
  );
};

export default Chat;
